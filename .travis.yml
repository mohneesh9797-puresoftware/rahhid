language: c

os: linux
dist: trusty
sudo: required

compiler:
    - clang
    - gcc

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -y libhidapi-dev

script:
    - make

after_success:
    - bash <(curl -s https://codecov.io/bash)
